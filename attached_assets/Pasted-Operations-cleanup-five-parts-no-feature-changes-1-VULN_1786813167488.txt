Operations cleanup, five parts, no feature changes.

1. VULNERABILITY TRIAGE: npm audit --omit=dev reports 6 critical / 23
   high. For each critical and high: identify the package, whether it's
   direct or transitive, and whether the vulnerable code path is
   reachable in our usage. Apply safe non-breaking upgrades
   (npm audit fix WITHOUT --force, plus targeted minor/patch bumps).
   For anything requiring a major-version bump, do NOT upgrade — list it
   in your summary with the breaking-change assessment. After changes:
   npm run lockfile:scrub, full test suite, build. Report before/after
   vulnerability counts.
2. ENV DOCS: add to .env.example with comments: SWAPS_ENABLED=false and
   SIGNALS_ENABLED=false (KILL SWITCHES — flipping is a human/legal
   decision), ZEROX_API_KEY= (0x Swap API, server-side only),
   TREASURY_ADDRESS= (Base wallet that receives swap fees — must be
   tested before enabling swaps), SWAP_FEE_BPS=30, DAILY_AI_BUDGET_USD=25.
3. REPO HYGIENE (finally): delete all root *.tar.gz (8) and root
   screenshot pngs; move every root .md except replit.md, README.md,
   DESIGN.md into docs/archive/; add attached_assets/ and *.tar.gz to
   .gitignore and git rm -r --cached attached_assets (files leave the
   repo index; Replit keeps the working copies). Confirm dev server
   boots after.
4. DEAD PAGES: pages AnalyticsDashboard, InsightsDashboard,
   defi-dashboard, gamification, nft-gallery, web3-wallet are imported
   by no route. For each: check git history/nav references to judge
   whether it was meant to be routed; if it duplicates a live feature or
   is abandoned, delete it; if it's a real feature that lost its route,
   restore the route and add it to the nav where it plausibly belongs.
   State the decision per page in your summary.
5. SWAP CAP PERSISTENCE: move riskEngine's in-memory swapDailyVolume to
   a swap_daily_volume table (wallet, day, volume_usd) mirroring the
   api_spend_daily pattern; add a restart-survival test.
Gate: lint, full tests, build, zero new tsc errors vs baseline. Push and
verify with git ls-remote.