Establish the StreamAiX design system foundation. This task creates tokens,
primitives, and documentation ONLY — do not restyle any pages yet.

IDENTITY: "The AI trading ledger" — dark ink surfaces, violet intelligence
accent, editorial serif display type. It must visually match the email
newsletter (see server/services/newsletterTemplate.ts for the shipped
palette).

1. TOKENS — in client/src/index.css, add under :root (keep existing shadcn
   vars working; these are additive):
   --ink-page: #080B14;      /* app background */
   --ink-surface: #10162A;   /* cards, panels */
   --ink-raised: #181F38;    /* hover states, nested surfaces */
   --ink-edge: #232B45;      /* borders */
   --ink-divider: #1A2138;   /* row separators */
   --text-primary: #F2F4FA;
   --text-body: #C9CEDC;
   --text-secondary: #9BA3B7;
   --text-muted: #7A8299;
   --accent: #8B7CF6;        /* buttons, active states */
   --accent-bright: #A99DF8; /* links, highlights on dark */
   --accent-deep: #6D5BE0;   /* pressed states */
   --gain: #3DD68C;
   --loss: #FF7B7B;
   --warn: #FFB454;
   Map ALL of these into tailwind.config.ts theme.extend.colors so classes
   like bg-ink-surface, text-secondary, border-ink-edge, text-gain work.

2. TYPE SCALE — add to the theme: fontFamily.display = Georgia + serif
   fallbacks (matches the newsletter masthead). Rule: font-display is for
   page titles and section headings ONLY; all UI/data stays on the existing
   sans. Numbers in data contexts get the .tabular class (font-variant-
   numeric: tabular-nums) — add that utility to index.css.

3. SHAPE + EFFECT RULES (encode in the primitives, not as suggestions):
   - Radii: rounded-xl for cards/inputs/buttons, rounded-2xl for modals/
     sheets. Nothing else. 
   - Exactly TWO sanctioned gradients, defined as utilities in index.css:
     .grad-accent (violet #8B7CF6 → #6D5BE0, for primary CTAs and the tour)
     .grad-surface (barely-there #10162A → #0C1122, for hero panels)
     No other gradients anywhere.
   - Glow/shadow: one token — .glow-accent (0 0 24px rgba(139,124,246,.25))
     used only on primary CTAs and active states.

4. PRIMITIVES — create in client/src/components/ds/ :
   - Surface.tsx: variants panel (ink-surface + ink-edge border + rounded-xl)
     and raised (ink-raised, for nesting). All cards in the app will become
     Surface.
   - StatValue.tsx: label (text-muted, 10px, tracking-widest, uppercase) +
     value (font-display, text-primary, tabular) + optional delta
     (gain/loss colored, always signed, toFixed(2)). This is the ONE way
     numbers are displayed.
   - SectionTitle.tsx: font-display text-primary with optional muted
     eyebrow line above.
   - LedgerRow.tsx: the three-column pattern from the newsletter (label
     left / value right-aligned tabular / delta right-aligned fixed-width)
     for any list of numeric rows.
   Reuse the existing PageHeader but restyle it with the tokens.

5. DESIGN.md — create at repo root. It is the CONTRACT for all future UI
   work. Contents: the token table with hex values and usage rules; the
   radii rule; the two-gradient rule; the type rules (display vs sans,
   tabular numbers); primitive usage examples; and a BANNED list: raw
   bg-slate-*, bg-gray-*, bg-zinc-*, bg-purple-*, bg-violet-*, bg-indigo-*,
   text-gray-*, rounded-lg, rounded-3xl, and any from-*/to-* gradient
   classes outside the two utilities. Add a rule to replit.md under Hard
   Rules: "All UI work must follow DESIGN.md. Banned classes list is
   enforced by npm run design:lint."

6. LINT GUARD — add scripts/design-lint.sh: greps client/src for the banned
   classes, prints file:line for violations, exits 1 if any are found in
   files that import from components/ds (so it only enforces on MIGRATED
   files for now — store the migrated-file list in scripts/design-migrated.txt,
   starting empty). Add "design:lint": "bash scripts/design-lint.sh" to
   package.json.

Finish: npm run check, npm test, npm run build all pass. No page files
modified. Push to origin main and verify with git ls-remote origin main.