Unify the landing page background into one continuous animated layer — no
per-section backdrops, no visible seams between sections.

1. In pages/landing.tsx: make the page root `relative bg-ink-page` and
   mount ONE <AmbientBackground /> as a `fixed inset-0 z-0
   pointer-events-none` layer (fixed, so the backdrop persists while
   scrolling). All page content wraps in a `relative z-10` container.

2. Make every landing section transparent: audit ALL components rendered
   by pages/landing.tsx (hero-section and every carousel/section component
   in components/landing/) and REMOVE their opaque full-bleed backgrounds
   — bg-ink-page, bg-ink-surface, or any solid/gradient background on the
   section's outermost wrapper. Sections become transparent; the page
   backdrop shows through continuously. Interior cards/panels KEEP their
   Surface styling — this changes section shells only, not content
   surfaces. Remove the hero-local <AmbientBackground /> mount from
   hero-section.tsx (it becomes redundant).

3. Absorb the particle network: the leaderboard/"17 AIs trading" section
   renders an animated particle-network backdrop. Extract that effect into
   AmbientBackground as a second layer (subtle, behind the orbs, full
   page) OR — if extraction is complex — keep it in that section but give
   it top and bottom fade masks (mask-image linear-gradient, ~120px
   transparent ramps) so it blends into the continuous backdrop with no
   hard edge. Choose whichever preserves the current effect's look; state
   which you chose.

4. Seam check: after the change, scroll the full landing page — there must
   be NO horizontal line where a background starts or stops. Any section
   needing extra emphasis may use grad-surface WITH fade masks, never an
   unmasked opaque block.

5. Contrast guard: with sections transparent, verify text still sits on
   sufficient contrast — where a section's text floated on its own dark
   background before, the backdrop's orbs may now pass beneath it. If any
   text area needs help, give that section an ink-page/70 backdrop-blur
   scrim (tokenized, add `.scrim` utility to index.css and DESIGN.md),
   not a return to opaque backgrounds.

6. Same treatment for pages/auth.tsx (root-level fixed mount, transparent
   content shell).

Zero logic changes; testids/handlers untouched. Update DESIGN.md's
expressive-layer section: "marketing pages use ONE page-level fixed
AmbientBackground; section shells are transparent; opaque section
backgrounds are banned on landing/auth." Gate: design:lint, tests, build,
zero new tsc errors. Push and verify with git ls-remote.