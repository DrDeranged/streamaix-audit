Rebuild client/src/components/OnboardingTour.tsx using the design system
from DESIGN.md and the ds/ primitives. The current tour is 15 steps with 15
different gradients — too long to finish. Compress to 6 steps WITHOUT
dropping any feature: related features merge into grouped steps rendered as
compact icon rows. Every current feature keeps its name, a tightened
one-line description, and its deep-link path.

NEW STRUCTURE (exactly these 6 steps):
1. "Welcome to StreamAiX" — value prop in two sentences (100 autonomous
   agents, prediction markets, earn STREAM points). Primary CTA "Show me
   around" + ghost "Skip tour".
2. "Find your way" — carousel swipe + sidebar nav + bottom dots, as three
   short instruction rows. (absorbs old Navigation step)
3. "Trade the markets" — grouped rows: Prediction Markets (YES/NO on real
   events), Bot Trading Simulator (stake on AI traders), AI Trading
   Intelligence (multi-factor signals). Each row: icon, name, one line,
   chevron linking to its page. (absorbs 3 old steps)
4. "Meet the AI ecosystem" — grouped rows: Knowledge Avatars (chat with AI
   experts), Live Streams & Debates (24/7 broadcasts), AI Content Processor
   (video → summary → market). (absorbs 3 old steps)
5. "Earn & learn" — grouped rows: STREAM points (earned on every activity),
   Bounty Feed (complete tasks for rewards), Portfolio Command Center
   (unified assets + tax analytics), Learning Hub (crypto & trading
   skills). (absorbs 4 old steps)
6. "Claim 2,500 STREAM" — the existing signup bonus CTA, restyled: Surface
   panel, grad-accent button with glow-accent, muted "Explore first" ghost
   link.

DESIGN RULES:
- One visual system for all 6 steps: Surface panel, SectionTitle with
  font-display, grad-accent ONLY on the primary button and the step-6 hero.
  Delete all 15 per-step gradient/glowColor fields from the step data.
- Progress: 6 dots, accent-filled for current/past, ink-edge for future,
  with "Step X of 6" in text-muted. Steps are tappable via the dots.
- Mobile-first: max-h with internal scroll for grouped rows; rows are
  44px+ touch targets; the panel is bottom-sheet style on small screens,
  centered modal (rounded-2xl) on desktop.
- Keep ALL existing behavior: localStorage/seen tracking, the skip logic,
  the deep-link navigation on action buttons, and any analytics events —
  verify each survives by reading the current implementation before
  rewriting.
- Copy discipline: descriptions max 9 words per feature row; step
  descriptions max 2 sentences. No exclamation marks except step 6.

Add the file to scripts/design-migrated.txt. Finish: npm run check, npm
test, npm run design:lint, npm run build all pass. Push to origin main and
verify with git ls-remote origin main.